{
  "name": "resume-analyzer-frontend",
  "private": true,
  "scripts": {
    "dev": "npx next dev .. --hostname 0.0.0.0 --port 3000",
    "build": "npx next build ..",
    "start": "npx next start .. --port 3000",
    "lint": "npx eslint .."
  }
}
